# PHP project ignores
.DS_Store
*.log
# Credenciales reales — nunca versionar. config/config.php solo tiene
# valores placeholder/getenv(); config/config.local.php trae los reales.
config/config.local.php
# Add any other sensitive or temporary files here
# Credenciales de produccion (Hostinger) — nunca versionar.
config/config.production.php
